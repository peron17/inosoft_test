# Setup

1. Build docker image for PHP
    ```bash
    $ docker build  -f Dockerfile . -t php-fpm8.0:1.0
    ```
2. Copy **.env.example** file to **.env**. If needed, please adjust .env file to your system environment.
3.
